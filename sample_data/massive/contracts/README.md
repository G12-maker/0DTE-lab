Local sample placeholder for Massive-style contract metadata.

The current simplified minute-level smoke does not require contract files yet,
but the directory is kept here so example configs point to a real path.
